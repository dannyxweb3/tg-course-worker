# 全仓库统一 LF。
#
# 这个项目要部署到 Ubuntu。文件带 CRLF 时，shell 脚本的 shebang 会变成
# "#!/usr/bin/env bash\r"，内核真的去找一个叫 "bash\r" 的程序，报错是：
#     /usr/bin/env: 'bash\r': No such file or directory
# 完全看不出是换行符的问题。systemd unit 和 nginx 配置同理，
# 带 \r 的值会被连着读进去，排查起来更费劲。
#
# text=auto 让 git 自动识别文本文件，eol=lf 保证工作区和仓库里都是 LF，
# 不管在谁的机器上 checkout。
* text=auto eol=lf

# 这几类在 Linux 上由内核或守护进程直接解析，一个 \r 就能让它们失效，
# 显式声明，不给平台留例外
*.sh       text eol=lf
*.service  text eol=lf
*.conf     text eol=lf
*.py       text eol=lf
Dockerfile text eol=lf

# 二进制，不要做任何换行转换
*.png binary
*.jpg binary
*.pdf binary
*.ico binary
